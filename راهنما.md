# راهنمای اپ «دفتر قرض»

این یک دفتر حساب نسیه برای صبحانه‌سرا است. روی گوشی نصب می‌شود، بدون اینترنت کار می‌کند،
و همه‌ی اطلاعات فقط روی همان گوشی می‌ماند. نه حساب کاربری دارد، نه رمز، نه سرور.

کل کار دو مرحله است:

1. **یک بار** پروژه را روی یک هاست رایگان بگذارید تا یک لینک بگیرید.
2. **یک بار** آن لینک را در سافاری آیفون باز کنید و به صفحه‌ی اصلی اضافه کنید.

بعد از این دو مرحله، دیگر تا همیشه به اینترنت نیازی نیست.

---

## بخش ۱ — هاست کردن رایگان (روی لپ‌تاپ ویندوز انجام می‌شود)

### روش الف: Netlify (ساده‌ترین راه — پیشنهاد می‌شود)

۱. اول یک حساب رایگان بسازید: به `app.netlify.com` بروید و روی **Sign up** بزنید.
با ایمیل ثبت‌نام کنید. رایگان است و هیچ‌جا شماره کارت نمی‌خواهد.

> چرا اول حساب بسازید؟ چون اگر بدون حساب فایل‌ها را رها کنید، لینک شما موقتی است و
> بعد از مدتی پاک می‌شود. با حساب رایگان، لینک برای همیشه می‌ماند.

۲. وارد حساب که شدید، به این آدرس بروید: `app.netlify.com/drop`

۳. پوشه‌ی `daftar-qarz` را از روی لپ‌تاپ **بکشید** و داخل کادر وسط صفحه **رها کنید**.

۴. چند ثانیه صبر کنید. یک لینک به شما می‌دهد، چیزی شبیه:
`https://sparkly-cat-12ab34.netlify.app`

۵. اگر خواستید اسم لینک را قشنگ‌تر کنید: در همان صفحه دنبال
**Site configuration → Change site name** بگردید و مثلاً بنویسید `daftar-qarz-man`.
لینک می‌شود `https://daftar-qarz-man.netlify.app`.

۶. این لینک را برای خودتان یک جایی یادداشت کنید یا به گوشی‌تان پیامک کنید.

### روش ب: GitHub Pages

۱. در `github.com` یک حساب رایگان بسازید.

۲. روی **+** بالای صفحه و بعد **New repository** بزنید. یک نام بگذارید (مثلاً `daftar-qarz`)،
گزینه‌ی **Public** را انتخاب کنید و **Create repository** را بزنید.

۳. در صفحه‌ی بعد روی **uploading an existing file** بزنید. حالا **محتویات داخل** پوشه‌ی
`daftar-qarz` را (یعنی خود فایل `index.html` و پوشه‌های `js` و `css` و `assets` — نه خود پوشه‌ی بیرونی)
بکشید و داخل صفحه رها کنید. پایین صفحه **Commit changes** را بزنید.

۴. بالای صفحه روی **Settings** بزنید، از ستون کنار **Pages** را انتخاب کنید.
زیر عنوان Branch، گزینه‌ی **main** و بعد **/ (root)** را انتخاب کرده و **Save** بزنید.

۵. یکی دو دقیقه صبر کنید و صفحه را تازه کنید. لینک شما بالای همان صفحه ظاهر می‌شود:
`https://نام-کاربری-شما.github.io/daftar-qarz/`

---

## بخش ۲ — نصب روی صفحه‌ی اصلی آیفون

**مهم: این کار حتماً باید با مرورگر Safari انجام شود.** با کروم یا مرورگرهای دیگر گزینه‌ی
افزودن به صفحه‌ی اصلی درست کار نمی‌کند.

۱. سافاری را باز کنید و لینکی که در بخش ۱ گرفتید را وارد کنید.

۲. صبر کنید تا صفحه کامل بالا بیاید (چند ثانیه طول می‌کشد، چون دارد فایل‌ها را برای حالت
آفلاین ذخیره می‌کند).

۳. پایین صفحه، دکمه‌ی **اشتراک‌گذاری** را بزنید — همان مربعی که یک فلش رو به بالا دارد.

۴. فهرست را به سمت پایین بکشید تا گزینه‌ی **Add to Home Screen**
(یا «افزودن به صفحه اصلی») را ببینید. رویش بزنید.

۵. بالا سمت راست **Add** (افزودن) را بزنید.

۶. تمام. آیکون فنجان چای با نام «دفتر قرض» روی صفحه‌ی اصلی گوشی‌تان آمد.
از این به بعد فقط روی همین آیکون بزنید — دیگر کاری با سافاری ندارید.

### آزمایش آفلاین بودن (یک بار انجامش بدهید تا خیالتان راحت شود)

۱. اپ را باز کنید و یک صاحب‌کار آزمایشی با یک سفارش ثبت کنید.
۲. گوشی را روی **حالت پرواز** بگذارید (وای‌فای و دیتا هر دو خاموش).
۳. اپ را کامل ببندید و دوباره از روی آیکون باز کنید.
۴. باید کامل باز شود، صاحب‌کار و سفارش سرجایشان باشند، و بتوانید سفارش جدید هم ثبت کنید.
۵. بعد حالت پرواز را خاموش کنید و آن صاحب‌کار آزمایشی را حذف کنید.

---

## بخش ۳ — پشتیبان‌گیری خودکار برود توی آیکلاود

اپ هر ۷ روز یک‌بار، **خودش و بدون پرسیدن**، هنگام باز شدن یک نسخه‌ی پشتیبان می‌سازد و دانلود می‌کند.
با یک تنظیم ساده در گوشی، این فایل‌ها مستقیم توی آیکلاود ذخیره می‌شوند:

۱. در گوشی به **Settings** (تنظیمات) بروید.
۲. **Apps** و بعد **Safari** را باز کنید.
(در آی‌اواس‌های قدیمی‌تر، مستقیم **Safari** را در فهرست تنظیمات پیدا می‌کنید.)
۳. **Downloads** را بزنید.
۴. **iCloud Drive** را انتخاب کنید.

از این به بعد هر پشتیبانی که اپ می‌سازد، خودش می‌رود توی آیکلاود و روی همه‌ی
دستگاه‌هایتان در دسترس است.

> **یک نکته‌ی صادقانه:** وب‌اپ‌های آیفون وقتی کاملاً بسته‌اند نمی‌توانند در پس‌زمینه کاری بکنند —
> این محدودیت خود آیفون است، نه این اپ. پس پشتیبان‌گیری خودکار دقیقاً در لحظه‌ای انجام می‌شود که
> اپ را باز می‌کنید و بیش از ۷ روز از پشتیبان قبلی گذشته باشد. چون شما تقریباً هر روز اپ را
> باز می‌کنید، عملاً هفته‌ای یک‌بار پشتیبان گرفته می‌شود.

هر پشتیبان دو فایل است:

| فایل | به چه درد می‌خورد |
|---|---|
| `daftar-qarz-....xlsx` | فایل اکسل خوانا؛ با اکسل یا Numbers بازش کنید و همه‌چیز را ببینید |
| `daftar-qarz-....json` | فایل بازیابی؛ اگر خواستید اطلاعات را به اپ برگردانید از این استفاده کنید |

---

## بخش ۴ — ارسال خودکار پشتیبان به تلگرام (اختیاری ولی خیلی توصیه می‌شود)

این روش از همه مطمئن‌تر است، چون هیچ لمسی از شما نمی‌خواهد و فایل جای دیگری هم ذخیره می‌شود.

### گام ۱: ساختن ربات

۱. تلگرام را باز کنید و در کادر جستجو بنویسید: `@BotFather`
(همانی که کنار اسمش تیک آبی دارد را انتخاب کنید.)
۲. **Start** را بزنید.
۳. بنویسید و بفرستید: `/newbot`
۴. می‌پرسد اسم ربات چه باشد؛ هرچه دوست دارید بنویسید، مثلاً: `پشتیبان دفتر قرض`
۵. می‌پرسد نام کاربری ربات چه باشد؛ باید انگلیسی باشد و آخرش `bot` بیاید، مثلاً:
`daftar_qarz_backup_bot`. اگر گفت تکراری است، چند رقم آخرش اضافه کنید.
۶. حالا یک پیام می‌دهد که داخلش یک رشته‌ی طولانی است، چیزی شبیه:
`8123456789:AAH8x-abcDEFghiJKLmnoPQRstuVWXyz1234`
این همان **توکن ربات** است. رویش انگشت بگذارید و کپی کنید.

### گام ۲: پیدا کردن شناسه چت

۱. **این گام را حتماً انجام دهید**: در همان پیام BotFather، روی لینک ربات خودتان
(مثلاً `t.me/daftar_qarz_backup_bot`) بزنید تا چت ربات باز شود و **Start** را بزنید.
اگر این کار را نکنید، ربات اجازه ندارد برایتان پیام بفرستد.
۲. حالا در جستجوی تلگرام بنویسید: `@userinfobot` و بازش کنید و **Start** را بزنید.
۳. یک پیام می‌دهد که در آن خطی به شکل `Id: 123456789` هست.
آن عدد، **شناسه چت** شماست. کپی‌اش کنید.

### گام ۳: وارد کردن در اپ

۱. در اپ «دفتر قرض»، بالا سمت چپ روی **تنظیمات** بزنید.
۲. تا پایین بروید به بخش **ارسال خودکار پشتیبان به تلگرام**.
۳. توکن را در کادر اول و شناسه چت را در کادر دوم بچسبانید.
۴. **ذخیره** را بزنید.
۵. **ارسال آزمایشی** را بزنید. اگر پیام سبز «ارسال آزمایشی موفق بود» دیدید، همه‌چیز درست است
و باید یک فایل کوچک در چت ربات‌تان آمده باشد.

> اگر تلگرام روی گوشی شما فقط با فیلترشکن باز می‌شود، هنگام ارسال خودکار هم باید فیلترشکن
> روشن باشد. اگر نبود، اشکالی ندارد: دانلود روی گوشی به‌هرحال انجام می‌شود.

---

## بخش ۵ — کار روزمره با اپ

- **ثبت سفارش امروز:** روی نام صاحب‌کار بزنید ← دکمه‌ی بزرگ پایین صفحه.
با دکمه‌های بزرگ `+` و `−` تعداد هر قلم را بزنید. جمع، زنده و همان لحظه بالای دکمه‌ی ذخیره دیده می‌شود.
- **ثبت سفارش دیروز:** در همان صفحه روی تاریخ بزنید و از تقویم، روز موردنظر را انتخاب کنید.
- **اشتباه کردید؟** در صفحه‌ی صاحب‌کار، هر سفارش دکمه‌ی **ویرایش** و **حذف** دارد.
- **تسویه:** در صفحه‌ی صاحب‌کار، دکمه‌ی سبز **تسویه حساب**. مبلغ را نشانتان می‌دهد و می‌پرسد
مطمئنید. بعد از تایید، حساب باز صفر می‌شود و آن سفارش‌ها می‌روند به بخش **تاریخچه‌ی تسویه‌ها**
که همان پایین صفحه است.
- **جستجو:** در صفحه‌ی اصلی، هم با نام صاحب‌کار، هم با نام استادکار، و هم با هر کدام از
شماره تلفن‌ها می‌توانید بگردید.
- **تغییر قیمت‌ها:** تنظیمات ← قلم‌های منو. **قیمت جدید فقط روی سفارش‌های جدید اثر دارد؛
حساب‌های قبلی با قیمت روز خودشان دست‌نخورده می‌مانند.**
- **قلم جدید:** تنظیمات ← افزودن قلم جدید. خودکار در فرم ثبت سفارش هم ظاهر می‌شود.

### بازگرداندن اطلاعات از پشتیبان

تنظیمات ← **بازیابی از فایل پشتیبان** ← فایل `.json` را از Files یا آیکلاود انتخاب کنید.
قبل از جایگزینی، تعداد صاحب‌کارها و سفارش‌های داخل فایل را نشانتان می‌دهد و می‌پرسد مطمئنید.

---

## بخش ۶ — چند نکته‌ی مهم

- **اطلاعات کجاست؟** فقط روی همین گوشی، در حافظه‌ی داخلی سافاری. برای همین پشتیبان‌گیری مهم است.
- **اپ را از صفحه‌ی اصلی پاک نکنید** مگر اینکه قبلش پشتیبان گرفته باشید. با پاک کردن اپ،
اطلاعات هم پاک می‌شود.
- **تاریخچه‌ی سافاری را پاک کردید؟** تا وقتی گزینه‌ی پاک‌کردن «داده‌های وب‌سایت‌ها» را نزنید،
اطلاعات اپ سرجایش می‌ماند. باز هم: پشتیبان بگیرید.
- **گوشی جدید گرفتید؟** لینک را در سافاری گوشی جدید باز کنید، به صفحه‌ی اصلی اضافه کنید،
و از طریق تنظیمات ← بازیابی، فایل `json` آخر را برگردانید.
- **می‌خواهید بعداً چیزی در اپ عوض شود؟** فایل‌ها را روی لپ‌تاپ ویرایش کنید، بعد در فایل `sw.js`
خط `var CACHE_NAME = 'daftar-qarz-v1';` را به `v2` تغییر دهید و دوباره روی Netlify رها کنید.
اگر این عدد را عوض نکنید، گوشی نسخه‌ی قدیمیِ ذخیره‌شده را نشان می‌دهد.

---

## ساختار فایل‌های پروژه

```
daftar-qarz/
├── index.html          صفحه‌ی اصلی اپ
├── manifest.json       نام و آیکون اپ برای افزودن به صفحه‌ی اصلی
├── sw.js               ذخیره‌ی فایل‌ها برای کار کردن بدون اینترنت
├── .nojekyll           فقط برای اینکه GitHub Pages فایل‌ها را دست‌کاری نکند
│
├── css/
│   └── styles.css      ظاهر اپ (رنگ‌ها، دکمه‌های بزرگ، چیدمان راست‌به‌چپ)
│
├── js/
│   ├── jalali.js       تقویم شمسی و قالب‌بندی اعداد فارسی
│   ├── store.js        نگه‌داری اطلاعات روی گوشی و همه‌ی محاسبه‌ها
│   ├── xlsx.js         ساخت فایل اکسل برای پشتیبان
│   ├── backup.js       پشتیبان‌گیری خودکار، دانلود، و ارسال به تلگرام
│   ├── ui.js           صفحه‌های اپ و رفت‌وآمد بینشان
│   └── app.js          راه‌اندازی اولیه
│
├── assets/
│   ├── fonts/          فونت وزیرمتن (تا اپ آفلاین هم فارسیِ خوانا داشته باشد)
│   └── icons/          آیکون فنجان چای در سه اندازه
│
└── tools/              فقط ابزار کار روی لپ‌تاپ؛ برای خود اپ لازم نیست
    ├── serve.js        اجرای اپ روی لپ‌تاپ برای آزمایش
    ├── make-icons.js   ساخت دوباره‌ی آیکون‌ها
    ├── test-logic.js   آزمون محاسبه‌ها
    └── test-browser.js آزمون کامل اپ در مرورگر
```

### اجرای اپ روی لپ‌تاپ برای آزمایش

اگر Node.js روی لپ‌تاپ نصب است، در پوشه‌ی پروژه این را اجرا کنید:

```
node tools/serve.js
```

بعد در مرورگر `http://localhost:8080` را باز کنید.
